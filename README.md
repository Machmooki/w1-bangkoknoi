# W1@Bangkoknoi

The live product app is in **[`web/`](./web/)** (Next.js + Sanity + Postgres enquiries).

Legacy static HTML in this folder is kept as visual/content reference only — do not deploy it.

```bash
cd web
cp .env.example .env.local
npm install
npm run dev
```